pipeline {
    agent {
        label 'linux1'
    }

    stages {
        stage('Example Stage') {
            steps {
                echo "Running on node: ${env.NODE_NAME}"
            }
        }
    }
}
