pipeline {
    agent {
        label 'linux1'
    }

    stages {
        stage('Run Scripts') {
            steps {
                sh 'chmod +x jenkins3 jenkins4 jenkins5 jenkins6'
                sh 'jenkins3'
                sh 'jenkins4'
                sh 'jenkins5'
                sh 'jenkins6'
            }
        }
    }
}
