pipeline {
    agent {
       label 'linux1'
     }

    parameters {
        string(name: 'NAME' , defaultValue:'Siddu')
    }

    stages {
        stage('Print') {
            steps {
                echo params.NAME
            }
        }
    }
}
