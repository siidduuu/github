pipeline {
    agent {
       label 'linux1'
     }

    stages {
        stage('Current day details') {
            steps {
                sh 'date'
                sh 'echo "current date is ${date}"'
            }
        }
    }
}
